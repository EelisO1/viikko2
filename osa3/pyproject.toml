[tool.poetry]
name = "osa3"
version = "0.1.0"
description = ""
authors = ["Eelis laptop <eelis.orhala@live.fi>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
